# Investigation Reflections

### [2026-09-12] WeTERM 子模块缺失时，可从本机 asar 只读核实行为，但必须标注安装版本
**背景**: xweterm 远端不可访问，主仓库主要是文档。WeTERM 3.5.7 的 asar 含独立的 ZMODEM
manager、上传/下载处理器和前端，足以核实功能；不能假装它就是主仓库锁定提交的源码。
**建议提升至**: skill，未来若创建 WeTERM 对照研究 skill，可加入这条定位方式；本轮不自动提升。

### [2026-09-12] 本仓库的 ZMODEM 协议互通测试不能替代真实 event loop 和 GUI 文件归属验证
**背景**: 44 项协议相关测试全绿，但测试直接调用 Session，绕开了全局下载缓冲、事件次序、
文件选择取消和 poll timer。后续必须把这些路径纳入验收，且要求对端正常退出。
**建议提升至**: skill，未来终端文件传输测试 skill 的验收规则；本轮不自动提升。
